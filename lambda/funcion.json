{
  "Description": "Example of prepalert-{{ env `PREPALERT_MODE` `webhook` }}",
  "Environment": {
      "Variables": {
          "PREPALERT_LOG_LEVEL": "{{ env `PREPALERT_LOG_LEVEL` `info`}}",
          "PREPALERT_MODE": "{{ env `PREPALERT_MODE` `webhook` }}"
      }
  },
  "FunctionName": "prepalert-{{ env `PREPALERT_MODE` `webhook` }}",
  "Handler": "bootstrap",
  "MemorySize": 128,
  "Role": "arn:aws:iam::{{ must_env `AWS_ACCOUNT_ID` }}:role/prepalert_lambda",
  "Runtime": "provided.al2",
  "Tags": {},
  "Timeout": 5,
  "TracingConfig": {
      "Mode": "PassThrough"
  }
}
